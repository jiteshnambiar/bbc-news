const assert = require('assert');
const bbcNews = require('../index.js');

describe('bbcNews', function () {
  it('has a test', function () {
    assert(false, 'bbcNews should have a test');
  });
});
